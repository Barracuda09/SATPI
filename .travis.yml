sudo: false
language: cpp
compiler:
  - g++

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
addons:
  apt:
    packages:
    - gcc-4.8
    - g++-4.8

script:
  - g++-4.8 --version
  - make
  - make clean
  - make debug
